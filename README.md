# Source code for <https://jeffalessandrelli.net>

## Install and run

### Initialize bundler

`bundle install`

### Configure bundler

`bundle install --path vendor/bundle`

### Serve the site locally at <http://localhost:4000>

`bundle exec jekyll serve`
